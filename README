Данный код представляет собой компонент-таблицу на основе jquery, bootstrap и  шаблонизатора mustache. 
Таблица поддерживает шаблоны ячеек.


Компонент взят из моего домашнего проекта - система по регистрации тасков по системе управления временем Pomodoro. 
Поэтому на demo-странице таблица имеет 3 колонки Описание таска, количество "помидоров" (интервалы по 25 минут), 
требуемых для завершения таска, и колонку с действием (ссылка удалить запись).

Чтобы запустить проект, выполните с коммандной строки (требуется nodejs):

  $ npm install
  $ bower install
  $ grunt preview

Откройте в браузере http://localhost:9000.