Данный код представляет собой компонент-таблицу на основе jquery, bootstrap и mustache template engine.
Таблица имеет 3 колонки Описание таска, количество "помидоров" (интервалы по 25 минут), требуемых для завершения таска, и колонку с действием (ссылка удалить запись).

Компонент "выдран" из моего личного проекта - система по регистрации тасков по системе управления временем Pomodoro. 
Весь код проекта не предоставил, так как писал проект, уделяя внимание функционалу, нежели коду.

Чтобы запустить проект выполните с коммандной строки (требуется nodejs):

  $ npm install
  $ bower install
  $ grunt preview

Откройте в браузере http://localhost:9000.